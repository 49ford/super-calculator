name: V6 Engine Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run accumulation engine test
        run: |
          node --version
          node tests/accumulation.test.mjs
``
